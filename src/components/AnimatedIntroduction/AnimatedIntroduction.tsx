import React from 'react';
import styles from './AnimatedIntroduction.module.css'; // Ensure to import or use inline styles

const AnimatedIntroduction: React.FC = () => {
  return (
    <ul className={styles.background}>
   <li></li>
   <li></li>
</ul>
  );
};

export default AnimatedIntroduction;
